    import React from "react";

    import { getPosts, getPostDetails } from '../src/components/Service/fetchPosts'

    import { PostDetail, CategoryWidget, PostWidget, Comments, CommentForm } from

    const PostDetails = () => {
        return(
            <>

            </>
        )
        
    }
    export default PostDetails;